---
description: 文档存储规则 - docs 下 PARA 式目录用途、命名与智能体行为（Claudian 仓库）
globs: **/*
alwaysApply: false
---

# S01-文档存储规则

## 核心原则

1. **方案先行（重大改动）**：新功能、较大重构或复杂 Bug 修复前，宜在 `docs/01-Projects/` 留下可追溯的需求/方案/变更记录。
2. **可追溯**：能回答「改了什么、为什么改、如何验收」。
3. **分类存放**：按文档性质选择 Projects / Areas / Resources / Archives。

## 目录用途

### docs/01-Projects/ — 项目与工作流产出

**何时使用**

- 新功能、较大改动、复杂问题闭环
- Cursor 工作流 C01～C08 产出的需求清单、问题报告、解决方案、审查报告、完工检查等

**推荐目录命名**

`R{YYYYMMDD}-{两位序号}-{主题}/`

示例：`R20260420-01-侧栏会话恢复优化/`

（与仓库内已有实践一致；同一天多个主题则序号递增 02、03…）

**目录内文件示例**

- `C01_需求清单_xxx.md`、`C02_问题报告_xxx.md`
- `C03-解决方案(初稿)_xxx.md`、`C04-方案审查报告_xxx.md` 等

**智能体行为**

- C01/C02：用户未指定路径时，**默认**在 `docs/01-Projects/` 下新建或选用上述格式的项目目录，再写入文档。
- 实施阶段（C06）：优先对照已确认的解决方案与验收标准；若有方案文档，提交前更新其变更/验收部分。

### docs/02-Areas/ — 领域知识长期维护

**何时使用**

- 最佳实践、流程规范、架构说明、fork 同步指南等**长期有效**内容

**智能体行为**

- 编码前可检索与本任务相关的 Areas 文档（如构建、发版、上游同步）。
- 规范正文在 `docs/02-Areas/`；可执行摘要可在 `.cursor/rules/`（本文件与 S02、S03、S00 等）。

### docs/03-Resources/ — 外部资料

**何时使用**

- 竞品调研、API 摘录、第三方链接合集

**要求**：尽量标注来源 URL 与摘录日期。

### docs/04-Archives/ — 归档

**何时使用**

- 周期性报告、历史快照、不再频繁更新但需保留的材料（如上游社区信号采集/调查报告）

## 文档流转（简图）

```text
需求/问题（C01/C02）→ 方案（C03）→ 审查（C04）→ 完善（C05）→ 实施（C06）→ 验收（C07）→ 发版（C08）
         ↓
   docs/01-Projects/R{日期}-{序号}-{主题}/
```

## 与 .cursor 规则的关系

| 人类可读详细说明 | 智能体规则文件 |
|------------------|----------------|
| Areas 中各类「最佳实践」「规范」 | `.cursor/rules/S*.mdc` |

不必强制「编号一一对应」，但**同一主题**应避免 Areas 与 rules 互相矛盾。

## 智能体检查清单

### 接到需求/问题时

- [ ] 是否写入 `docs/01-Projects/` 下合适目录（用户未指定路径时按 `R{YYYYMMDD}-{序号}-{主题}/`）
- [ ] 文件名是否遵循 C01～C08 约定前缀

### 提交代码时

- [ ] 重大变更是否能在 `docs/01-Projects/` 或提交说明中追溯
- [ ] 是否误提交临时笔记（应放 `.context/` 等忽略路径，见 `CLAUDE.md`）

## 相关规则

- `S00-Claudian-工作区上下文.mdc`
- `S02-构建系统规则.mdc`
- `S03-版本发布规则.mdc`
