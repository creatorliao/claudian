---
report_type: upstream-community-signal-analysis
analysis_date: 2026-04-22
basis_raw: "[[上游社区信号_采集_2026-04-22_160000]]"
upstream: YishenTu/claudian
upstream_main_tip: c552433
note: "本地 Cursor 环境未能可靠执行 git；以下「相对 a26d318 的缺口」用 GitHub API 核对。合并决策前务跑 git fetch 与 git log HEAD..upstream/main。"
---

# 上游社区信号调查报告（定稿）

> 研判依据：`docs/02-Areas/最佳实践_fork-同步上游与社区信号筛选.md`。原始采集见 2026-04-22 采集文件 wikilink。**本文仅为信息研判与建议，不执行任何合并或改历史操作。**

## 摘要

1. **相对 2026-04-20 时记录的 merge-base `a26d318`，上游 `main` 又前进了 3 个提交**：`#528` 渲染/计时器与 thinking 同步、**2.0.4** 发版、`#548` Claude 侧 **上下文占用百分比与所选模型对齐**（含 `transformClaudeMessage` / `models.ts` 等较大逻辑与单测补充）。若本地仍未合并上游，这是当前最值得一次性跟进的「代码包」。
2. **新出现的社区 Issue 信号（约 4/21 起）**：WSL 下 Codex app-server 退出（#546）、**切出 Obsidian 窗口会暂停对话**（#549）、**outputStyle 未进 system prompt**（#544）、Codex 配置方式提问（#547）。前两条偏环境与生命周期；#544 属 Claude Code 行为对齐，与「同步上游发版线」可一并评估。
3. **新 Open PR**：[#545](https://github.com/YishenTu/claudian/pull/545) 注册「切换到第 1～9 个 tab」的 Obsidian 命令（含测试），面相对独立，**合并成本低于** #484 / #456 等提供商大 PR；可按需关注。
4. **未进 `main` 的修复线**：#535（结构化 `tool_result` / #534）与 #516（AskUserQuestion 默认自由文本）仍在 Open，与 4/20 快照一致，可继续排期或等上游合入再拉整包。

## P0（建议优先关注）

| 类型 | 条目 | 理由 |
|------|------|------|
| 上游提交（相对 `a26d318`） | `c552433` #548，及前面的 `f365a2d` #528、`f462c40` 2.0.4 | 与官方 **2.0.4 发版线** 一致；#548 直接改善 Claude 模型与 context usage 显示一致性，和对话体验强相关。 |
| Issue 风险 | #546、#539 | WSL/跨环境与 Codex app-server/初始化超时，若你方用户用 Codex + Windows 生态，需对照是否已被本地改动覆盖。 |
| Issue 与缺口呼应 | #449、#528 线 | Thinking 阶段切 Tab 仍有多条讨论；#528 已进上游，合入后应用本地验证是否仍复现。 |

## P1（值得排期）

| 类型 | 条目 | 理由 |
|------|------|------|
| PR | #535 + Issue #534 | Codex/MCP 结构化 `tool_result` 崩溃路径；有对应 PR，合并上游或 cherry-pick 前可对比 diff 规模。 |
| PR | #516 + #487 | 与 AskUserQuestion 行为对齐 CLI，**改动面一般小于** 新提供商 PR。 |
| PR | #545 | Tab 热键/命令注册，**独立功能**，可评估与本地 Tab 管理是否冲突。 |
| PR | #530、#526 | 输入区发送/停止、行号 @mention 等体验增强，需评估与本地输入栈冲突。 |
| Issue | #549 | 多窗口/失焦时暂停，可能是 Electron/Obsidian 可见性与定时器策略问题，适合记录复现后对照上游。 |

## P2（观望 / 大改动 / 按需）

| 类型 | 条目 | 理由 |
|------|------|------|
| PR | #484、#456、#390、#424 等 | 新提供商或大块功能，**维护成本高**；非明确需要则等上游或长期 Open 再议。 |
| Issue | #540、#547、#495 | 功能建议、提问类、auto mode 等，不自动转化为合并任务。 |
| Issue | 带 `stale` 的 Open | 与最佳实践一致，默认不自动进本地待办。 |

## 代码缺口清单（与历史 merge-base 对照）

> **前提**：你本地若仍为 4/20 报告时的分歧关系（`merge-base` = `a26d318`），则下列 3 条为上游已具备、你侧待评估合入的提交。**若已执行过 `merge upstream` 或 cherry-pick，条数会变化**；以 `git log HEAD..upstream/main` 为准。

| SHA（短） | 说明 |
|-----------|------|
| `f365a2d` | fix: patch renderer timers and thinking sync (#528) |
| `f462c40` | 2.0.4（manifest / package 版本号） |
| `c552433` | fix: align claude context usage with selected model (#548) |

- **GitHub 对比**：[a26d318...c552433](https://github.com/YishenTu/claudian/compare/a26d318bbd130434970e22c305c47498549f420d...c552433ffbc750ef7c8e28f490f9e27e9fda1996)（`ahead_by: 3`）

## 社区快照（与 Windows / WSL / Codex / Node 24）

| 编号 | 一句话信号 |
|------|------------|
| #546 | WSL Ubuntu 中 Codex app-server 进程退出，Node 24 + Claudian 2.0.3 环境。 |
| #549 | 离开 Obsidian 窗口时对话被暂停，与「后台跑完」预期不一致。 |
| #544 | `outputStyle` 与项目级 output-styles 未进入 Claudian 的 system prompt 组装。 |
| #541 | Node v24 下 `setMaxListeners` 与 abortSignal 兼容性。 |
| #524、#533、#496 | Claude Code 2.1.x 与 Windows 上 CLI 参数、tool_use 等（延续）。 |

## 建议的下一步（仅建议，需在本机执行）

1. 在本机仓库根目录执行：`git fetch upstream`，再运行 `git merge-base HEAD upstream/main` 与 `git log --oneline -n 30 HEAD..upstream/main` **刷新真相**；可选运行 `npm run report:upstream-signals` 覆盖采集文件。
2. 若需对齐官方 2.0.4 与 #548 / #528：优先 **整包 `merge upstream/main`** 或按提交顺序 **cherry-pick**，再跑项目的 `typecheck` / `lint` / `test` / `build`。
3. 对 #534/#535：在本地 `rg`/日志确认是否已含 `extractToolResultContent` 同类修复；无则纳入下一轮。
4. 对 #545：若多 Tab 工作流是 fork 用户高频场景，可单独审 PR diff 再决定是否合入或等上游。

## 自检（最佳实践第 7 节）

- [x] 已标明「相对 `a26d318`」与 `upstream main` tip，并提醒用 `HEAD..upstream/main` 终检  
- [x] 未将「Issue 关闭」与「本 fork 已好」划等号（本报告未用关闭项替代缺口）  
- [x] 已区分已进 `main` 的提交与仍 Open 的 PR  
- [x] 价值段落已分 P0/P1/P2 并附理由  
