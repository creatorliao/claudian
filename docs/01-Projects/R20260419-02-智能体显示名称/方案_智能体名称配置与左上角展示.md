# 方案：智能体名称配置与左上角展示

## 目标

在设置中新增一项与「Claudian 应该如何称呼你？」（`userName`）同类的纯 UI 偏好：**智能体显示名称**。用户可自定义对话界面左上角（Logo 旁）展示的名称；配置需持久化到 `.claudian/claudian-settings.json`，与现有设置读写路径一致。

## 现状简述

- **用户称呼**：字段 `userName`，在 `ClaudianSettingTab` 的「内容」分组下通过 `Setting.addText` 编辑，`onChange` 中写入 `plugin.settings` 并 `saveSettings()`。
- **左上角文案（实施后）**：`ClaudianView.buildHeader` 中 `titleTextEl` 使用 `getResolvedAgentDisplayName()`（`agentName` 非空则用之，否则回退 **`Claudian`**），与 Logo 同处 `claudian-title-slot`；设置保存后通过 `syncAgentDisplayNameInHeader()` 即时刷新。

## 设计决策

| 项目 | 说明 |
|------|------|
| 字段名 | `agentName`（字符串），与 `userName` 并列，语义为「界面展示用智能体名称」，不写入系统提示词、不影响模型行为。 |
| 默认值 | 空字符串 `''`。 |
| 展示规则 | `agentName.trim()` 非空则用该字符串；否则回退为 **`Claudian`**，与当前产品默认一致。 |
| 设置 UI | 紧接在「如何称呼你」下一行：标题/说明/单行输入，交互与 `userName` 相同（`onChange` + `saveSettings`）。无需 `blur` 时重启服务（与系统提示词类设置不同）。 |
| 即时刷新 | 保存后对所有已打开的 `ClaudianView` 调用公开方法刷新标题文本，避免用户必须重开视图。 |
| 工作区标签文案 | `ClaudianView.getDisplayText()` 与左上角保持一致，便于侧栏/主标签标题与品牌一致（可选增强，本方案一并实现）。 |

## 实现清单

1. **类型与默认值**  
   - `ClaudianSettings` 增加 `agentName: string`。  
   - `DEFAULT_CLAUDIAN_SETTINGS` 中 `agentName: ''`。

2. **持久化**  
   - 与现有 `ClaudianSettingsStorage.load` 的 `{ ...getDefaults(), ...stored }` 合并逻辑兼容，旧配置文件无该键时自动使用默认值。

3. **设置页**  
   - `ClaudianSettings.ts`：在 `userName` 的 `Setting` 之后新增 `agentName` 的 `Setting.addText`。  
   - `onChange`：`this.plugin.settings.agentName = value` → `await this.plugin.saveSettings()` → `this.plugin.getAllViews().forEach((v) => v.syncAgentDisplayNameInHeader())`。

4. **聊天视图**  
   - `ClaudianView`：抽取解析后的展示名；`buildHeader` 创建 `titleTextEl` 后调用同步方法；实现 `syncAgentDisplayNameInHeader()`（供设置页调用）；`getDisplayText()` 使用同一解析逻辑。

5. **国际化**  
   - `i18n/types.ts` 增加 `settings.agentName.name` / `settings.agentName.desc`。  
   - 各 `locales/*.json` 在 `settings.userName` 后增加 `agentName` 条目（英文为基准，其余语言提供对应翻译）。

6. **测试与类型**  
   - 若存在手写完整 `ClaudianSettings` 字面量的单测，补充 `agentName: ''` 以满足类型检查。

## 验收要点

- 设置「内容」中可见新项，输入保存后 JSON 中出现 `agentName`。  
- 清空该项后左上角恢复为 `Claudian`。  
- 多标签、Header 模式下原有显示/隐藏 Logo+标题逻辑不变（仍由 `updateTabBarVisibility` 控制）。  
- 未打开聊天视图时仅持久化即可；打开后修改设置应立即看到标题变化。

## 分支

实现工作于 Git 分支：`feat/agent-display-name`（若已存在则在该分支上迭代）。

---

## 关联文档与清单（扫描同步）

- **差异清单**：《[差异清单_国际化与配置扫描.md](../R20260419-01-国际化与配置扫描/差异清单_国际化与配置扫描.md)》  
  - **第二节** `S4`（标签上限 `Notice`）、`S5`（顶栏标题与 `agentName` 索引）。  
  - **第九节**：`agentName` 已实现项、**N1**（默认 `Claudian` 硬编码与可选本地化）、**N2**（语言精简后仅维护 en/zh-CN 的 `settings.agentName`）。  
- **国际化总方案**：《[方案_国际化仅中英与智能体输入占位符.md](../R20260419-01-国际化与配置扫描/方案_国际化仅中英与智能体输入占位符.md)》**§4.1**，约定语言清理后本功能只保留中英翻译文件中的 `agentName` 条目。

### 国际化（修订说明）

上文「实现清单」第 5 条写「各 `locales/*.json`」：在采用 **仅 English + 简体中文** 策略后，**以 `en.json`、`zh-CN.json` 为唯一维护目标**；其余语言文件删除后，不再为 `agentName` 增加翻译。

### 实现状态（仓库扫描摘要，便于对照方案）

| 项 | 状态 |
|----|------|
| `ClaudianSettings` + `t('settings.agentName.*')` + `syncAgentDisplayNameInHeader` 循环 | 已与方案一致 |
| `ClaudianView` `getResolvedAgentDisplayName` / `getDisplayText` | 已与方案一致 |
| 默认回退 `'Claudian'` | 硬编码；可选本地化见差异清单 N1 |

### 追溯：界面语言精简（2026-04-19）

与 **N2**（仅维护 en/zh-CN 的 `settings.agentName`）配套的 **产品级决策** 已写入《**[方案_国际化仅中英与智能体输入占位符.md](../R20260419-01-国际化与配置扫描/方案_国际化仅中英与智能体输入占位符.md)**》**§8**，摘要如下：

- 语言包仅 **`en.json` / `zh-CN.json`**；**默认 `locale` 为 `zh-CN`**。  
- 旧配置中非 `en` / `zh-CN` 的 `locale` 经 **`normalizeClaudianLocale`** 归并为 **`zh-CN`** 并持久化。  
- 中文包缺键时 **回退英文词条**（`FALLBACK_LOCALE = 'en'`）。  

本方案中 **智能体显示名称** 的翻译键 **`settings.agentName.*`** 只需在上述两个语言文件中维护；与「十种语言」时期相比，维护面已收敛。

### 关联决策：主聊天输入框占位符（2026-04-19）

与左上角品牌/名称展示同属 **对话壳体验**，但实现位置不同，在此单独记录以便追溯：

| 决策 | 说明 |
|------|------|
| **默认无占位文案** | 主输入区（`Tab.ts` → `buildTabDOM` 内 `textarea`）**不设置** `placeholder`，空输入时不再出现 *How can I help you today?* 等任何提示。 |
| **原则** | 输入框是用户向智能体下达指令的入口，**禁止**使用助手寒暄式、客服开场白式占位语（中英文均避免）；与《**[方案_国际化仅中英与智能体输入占位符.md](../R20260419-01-国际化与配置扫描/方案_国际化仅中英与智能体输入占位符.md)**》**§2 选项 A** 一致。 |
| **例外** | `#` 指令模式、`!` Bash 模式等 **临时切换** 的占位符仍由各自 Manager 管理；流内 Inline 提问/计划等控件不在此条范围内。 |

详细原则与差异清单 **R1** 对照见《[方案_国际化仅中英与智能体输入占位符.md](../R20260419-01-国际化与配置扫描/方案_国际化仅中英与智能体输入占位符.md)》**§8.5**。
